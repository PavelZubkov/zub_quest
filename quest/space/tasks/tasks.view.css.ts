namespace $.$$ {

	const { rem } = $mol_style_unit

	$mol_style_define( $zub_quest_space_tasks, {

		Body: {
			padding: $mol_gap.block,
		},

		flex: {
			basis: rem(40),
		},

		Foot: {
			padding: $mol_gap.block,
		},

	} )

}
